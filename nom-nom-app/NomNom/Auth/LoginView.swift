import SwiftUI

/// Email/password sign-in (SPEC.md §4). Implemented in Milestone 1.
struct LoginView: View {
    var body: some View {
        ScaffoldPlaceholder(
            title: "Login",
            specNote: "Milestone 1: email + password → AuthModel.signIn; link to Sign Up."
        )
    }
}
